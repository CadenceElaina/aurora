# Session progress — 2026-05-30

Branch: `claude/awesome-goldberg-7kH2b` (PR #1). All work below is committed and pushed.
Baseline gate: `npx tsc --noEmit` clean · `npm test` → **273 passing (12 files)**.

## ⚠️ First thing on a new machine
1. `npm install`
2. Bring the DB up to date — **`drizzle-kit push` is currently broken** on this project's DB
   (crashes introspecting a pre-existing CHECK constraint: `TypeError ... reading 'replace'`
   in `drizzle-kit/bin.cjs`). Until that's fixed, apply schema via the recorded DDL:
   ```
   psql "$DATABASE_URL" -f scripts/sql/2026-05-30_daily_session_and_strategy.sql
   ```
   That file is idempotent and matches drizzle's naming, so a future fixed push diffs clean.
3. `npx tsc --noEmit && npm test` — expect 273 passing.

## What shipped this session
- **Doc sync** — committed `docs/files/SYSTEM_OVERVIEW.md` (sanitized: no research/IRB content),
  reconciled `docs/TASKS.md`, and fixed stale notes in PACING_SYSTEM / CONSTANTS / ADAPTIVE_SRS.
- **T3-A** — session view renders reviews before new problems.
- **T3-B** — overflow: unused review slots become new only when the queue is empty
  (pure `computeSessionComposition` in `capacity.ts` + tests).
- **T-030** (showcase blocker) — **server-backed daily session** for cross-day integrity:
  - `daily_session` table; pure `src/lib/session.ts`; `/api/session` GET/POST/PATCH.
  - Dashboard hydrates from the server; stable per-day target; acted-on ID **sets** (no
    double-count); completion gated on a persisted flag (no re-celebrate on reload / 2nd device);
    defer shrinks the plan. Demo stays in-memory.
- **T4-B** — RED-zone "triage now" branch in `computePracticeRecommendation` (ratio > 2.0).
- **T4-A + strategy foundation** — `users.strategy` is now an **explicit persisted mode**
  (`push_coverage`/`balanced`/`lock_in_retention`), set at onboarding, validated by
  `/api/user/settings`. Push Coverage orders reviews **FIFO** (`orderByLastReviewedAsc`).
  This supersedes the old "strategy is a preset, no migration" invariant (CLAUDE.md #5 updated).

## Verification gaps (need a logged-in browser — only the human can do these)
- T-030 end-to-end: log a review → reload (no re-celebrate) → next day fresh session.
- Strategy/FIFO: confirm Push Coverage shows oldest-reviewed-first in the live app.
Everything underneath is unit-tested, builds, and passes a demo smoke (`/dashboard` 200,
`/api/session` 401 unauth).

## Open follow-ups (not yet done)
- **Settings-panel strategy selector** — let existing users switch mode post-onboarding
  (they currently default to `balanced`; only onboarding sets strategy). Applying a mode
  should also set the `newPerSession` + `advisoryThreshold` preset.
- **Fix `drizzle-kit push`** introspection crash (so DDL files aren't needed).
- **`/api/user/settings`** has no automated test for the new `strategy` validation.
- Remaining plan tasks: **T4-C** (Lock In Retention ramp-up), Tier 5 (onboarding/settings),
  Tier 6 (surfaces). See `docs/TASKS.md`.

## Notes
- `.env.local` (with `DATABASE_URL`) is git-ignored and machine-local — set it on each machine.
- `next-env.d.ts` flips between `.next/types` and `.next/dev/types` depending on whether you
  last ran `build` or `dev`; don't commit that churn.
